Place the ECOSA logo image file in this folder so the app serves it at `/ecosa-logo.png`.

Steps:
1. Save your logo image as `ecosa-logo.png` (recommended PNG, ~200-800px wide).
2. Put the file in `ECOSA-frontend/public/`.
3. Restart the dev server (Vite will pick it up automatically on refresh).

If you want me to add the image file for you, paste the image as a base64 string here and I can create `public/ecosa-logo.png` for you.
